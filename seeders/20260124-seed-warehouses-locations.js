'use strict';

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // Data is already seeded in migrations/20260124-create-warehouse-location-tables.js
  },

  down: async (queryInterface, Sequelize) => {
    // Handled in migration down
  }
};
