import React from 'react';

interface POSSidebarProps {
  isCollapsed?: boolean;
  toggleSidebar?: () => void;
}

const POSSidebar: React.FC<POSSidebarProps> = ({ isCollapsed, toggleSidebar }) => {
  // Stub component — will be implemented with full sidebar later
  return null;
};

export default POSSidebar;
